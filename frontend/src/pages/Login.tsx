import { PagePlaceholder } from '@/components/common/PagePlaceholder'

export function LoginPage() {
  return (
    <PagePlaceholder
      title="Entrar"
      phase="Fase 4"
      description="Acesso do administrador. As credenciais vêm de variáveis de ambiente e a senha nunca fica no frontend."
    />
  )
}
